apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-redis
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: "billing-redis.dev.lan"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: billing-redis
            port:
              number: 80
