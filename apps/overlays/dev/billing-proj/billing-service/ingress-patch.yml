apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-service
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: "billing-service.dev.lan"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: billing-service
            port:
              number: 80
