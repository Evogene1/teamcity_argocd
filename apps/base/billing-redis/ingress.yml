apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-redis
spec:
  rules:
  - host: "placeholder.local"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: billing-redis
            port:
              number: 80
