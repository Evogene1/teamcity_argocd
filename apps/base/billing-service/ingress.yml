apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-service
spec:
  rules:
  - host: "placeholder.local"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: billing-service
            port:
              number: 80
